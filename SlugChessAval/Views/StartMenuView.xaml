<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:SlugChessAval.ViewModels;assembly=SlugChessAval"
        	   xmlns:views="clr-namespace:SlugChessAval.Views"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="SlugChessAval.Views.StartMenuView">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="auto" />
      <RowDefinition Height="48" />
      <RowDefinition Height="48" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <TextBlock Text="SlugChess" FontSize="48" TextAlignment="Center" Margin="10"/>
    <Button Grid.Row="1" Content="Login" Command="{Binding MoveToLogin}" />
    <Button Grid.Row="2" Content="Exit" Command="{Binding Exit}"/>
  </Grid>
</UserControl>
